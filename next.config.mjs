/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["raw.githubusercontent.com" , "github.com"], // allow GitHub raw image hosting
  },
};

export default nextConfig;
